apiVersion: v1
kind: Service
metadata:
  name: {{ include "market-frontend.fullname" . }}
  labels:
    {{- include "market-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "market-frontend.selectorLabels" . | nindent 4 }}
