apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "admin-wallet-api.fullname" . }}-test-connection"
  labels:
    {{- include "admin-wallet-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "admin-wallet-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
