apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-wallet-worker.fullname" . }}
  labels:
    {{- include "admin-wallet-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "admin-wallet-worker.selectorLabels" . | nindent 4 }}
