apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-wallet-worker.fullname" . }}
  labels:
    {{- include "user-wallet-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-wallet-worker.selectorLabels" . | nindent 4 }}
