apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "user-wallet-worker.fullname" . }}-test-connection"
  labels:
    {{- include "user-wallet-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "user-wallet-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
